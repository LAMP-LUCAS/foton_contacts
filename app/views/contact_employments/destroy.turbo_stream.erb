<%= turbo_stream.replace "contact_career_history" do %>
  <% 
  # We need to reload the association to reflect the deletion
  @contact_employments = @contact.employments_as_person.reload.includes(:company)
  %>
  <%= render partial: "contacts/show_tabs/career_history", locals: { contact: @contact, contact_employments: @contact_employments } %>
<% end %>